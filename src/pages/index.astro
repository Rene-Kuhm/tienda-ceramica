---
import Layout from '../shared/layouts/Layout.astro';
import Header from '../shared/components/Header.astro';
import Footer from '../shared/components/Footer.astro';
import { getCollection } from 'astro:content';

const products = await getCollection('products');
const featuredProducts = products.filter(p => p.data.featured).slice(0, 3);
const categories = await getCollection('categories');
---

<Layout title="ArteNatural - Cerámicas, Hierro y Aromaterapia">
  <Header />

  <main>
    <!-- Hero Section -->
    <section class="relative bg-white">
      <div class="max-w-7xl mx-auto px-8 pt-20 pb-32">
        <div class="text-center max-w-4xl mx-auto">
          <!-- Main headline -->
          <h1 class="text-6xl md:text-7xl lg:text-8xl font-semibold tracking-tight text-gray-900 mb-6">
            Artesanía que inspira
          </h1>

          <!-- Subheadline -->
          <p class="text-xl md:text-2xl text-gray-600 mb-12 max-w-3xl mx-auto">
            Piezas únicas de cerámica, hierro forjado y aromaterapia natural.
            Diseñadas con pasión, creadas para durar.
          </p>

          <!-- CTA Buttons -->
          <div class="flex flex-col sm:flex-row gap-4 justify-center items-center mb-16">
            <a
              href="/catalogo"
              class="w-full sm:w-auto px-8 py-3 bg-black text-white rounded-full text-sm font-medium hover:bg-gray-800 transition-colors"
            >
              Explorar colección
            </a>
            <a
              href="#productos"
              class="w-full sm:w-auto px-8 py-3 text-blue-600 text-sm font-medium hover:text-blue-700 transition-colors"
            >
              Ver productos destacados
            </a>
          </div>

          <!-- Hero Image Area -->
          <div class="relative w-full aspect-[16/9] bg-gray-100 rounded-3xl overflow-hidden shadow-apple-xl">
            <div class="absolute inset-0 flex items-center justify-center">
              <div class="text-9xl opacity-20">🏺</div>
            </div>
          </div>
        </div>
      </div>
    </section>

    <!-- Categories Section -->
    <section id="categorias" class="py-24 bg-gray-50">
      <div class="max-w-7xl mx-auto px-8">
        <div class="text-center mb-20">
          <h2 class="text-5xl md:text-6xl font-semibold tracking-tight text-gray-900 mb-4">
            Explora nuestras colecciones
          </h2>
          <p class="text-xl text-gray-600">
            Cada pieza cuenta una historia
          </p>
        </div>

        <div class="grid grid-cols-1 md:grid-cols-3 gap-6">
          {categories.map((category) => (
            <a
              href={`/catalogo/${category.data.slug}`}
              class="group block"
            >
              <div class="bg-white rounded-2xl overflow-hidden shadow-apple hover:shadow-apple-lg transition-shadow duration-300">
                <!-- Image Area -->
                <div class="relative aspect-[4/3] bg-gray-100 overflow-hidden">
                  <div class="absolute inset-0 flex items-center justify-center">
                    <span class="text-7xl transform group-hover:scale-110 transition-transform duration-500">
                      {category.data.slug === 'ceramica' && '🏺'}
                      {category.data.slug === 'hierro' && '⚒️'}
                      {category.data.slug === 'aromaterapia' && '🌿'}
                    </span>
                  </div>
                </div>

                <!-- Content -->
                <div class="p-6">
                  <h3 class="text-2xl font-semibold text-gray-900 mb-2">
                    {category.data.name}
                  </h3>
                  <p class="text-gray-600 text-sm mb-4">
                    {category.data.description}
                  </p>
                  <div class="text-blue-600 text-sm font-medium flex items-center gap-2 group-hover:gap-3 transition-all">
                    <span>Ver más</span>
                    <span class="transform group-hover:translate-x-1 transition-transform">→</span>
                  </div>
                </div>
              </div>
            </a>
          ))}
        </div>
      </div>
    </section>

    <!-- Featured Products -->
    <section id="productos" class="py-24 bg-white">
      <div class="max-w-7xl mx-auto px-8">
        <div class="text-center mb-20">
          <h2 class="text-5xl md:text-6xl font-semibold tracking-tight text-gray-900 mb-4">
            Productos destacados
          </h2>
          <p class="text-xl text-gray-600">
            Lo mejor de nuestra colección artesanal
          </p>
        </div>

        <div class="grid grid-cols-1 md:grid-cols-3 gap-6">
          {featuredProducts.map((product) => (
            <a
              href={`/producto/${product.id}`}
              class="group block"
            >
              <div class="bg-white rounded-2xl overflow-hidden hover:shadow-apple-xl transition-shadow duration-300">
                <!-- Product Image Area -->
                <div class="relative aspect-square bg-gray-50 overflow-hidden">
                  <div class="absolute inset-0 flex items-center justify-center">
                    <span class="text-8xl transform group-hover:scale-105 transition-transform duration-500">
                      {product.data.category === 'ceramica' && '🏺'}
                      {product.data.category === 'hierro' && '⚒️'}
                      {product.data.category === 'aromaterapia' && '🌿'}
                    </span>
                  </div>

                  <!-- Category badge -->
                  <div class="absolute top-4 right-4 px-3 py-1.5 bg-white/90 backdrop-blur-sm rounded-full">
                    <span class="text-xs font-medium text-gray-700">
                      {product.data.category === 'ceramica' && 'Cerámica'}
                      {product.data.category === 'hierro' && 'Hierro'}
                      {product.data.category === 'aromaterapia' && 'Aromaterapia'}
                    </span>
                  </div>
                </div>

                <!-- Product Info -->
                <div class="p-6">
                  <h3 class="text-xl font-semibold text-gray-900 mb-2">
                    {product.data.name}
                  </h3>
                  <p class="text-gray-600 text-sm mb-4 line-clamp-2">
                    {product.data.description}
                  </p>

                  <div class="flex items-center justify-between">
                    <span class="text-2xl font-semibold text-gray-900">
                      ${product.data.price.toLocaleString('es-AR')}
                    </span>
                    <div class="text-blue-600 text-sm font-medium flex items-center gap-2 group-hover:gap-3 transition-all">
                      <span>Comprar</span>
                      <span class="transform group-hover:translate-x-1 transition-transform">→</span>
                    </div>
                  </div>
                </div>
              </div>
            </a>
          ))}
        </div>

        <!-- CTA -->
        <div class="text-center mt-16">
          <a
            href="/catalogo"
            class="inline-block px-8 py-3 bg-black text-white rounded-full text-sm font-medium hover:bg-gray-800 transition-colors"
          >
            Ver catálogo completo
          </a>
        </div>
      </div>
    </section>
  </main>

  <Footer />
</Layout>
